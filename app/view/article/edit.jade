
h2
    span(v-if="edit") へんしゅう
    span(v-if="!edit") あたらしい
    
p ※俺用
p(v-show="!article") よみこみちう

form(v-on="submit: performUpdate($event)" v-if="article")

    .control-group
        input.control(v-model="article.title" placeholder="たいとる")
    .control-group
        input.control(v-model="article.digest" placeholder="ようやく")
    .control-group
        input.control(type="checkbox" v-model="article.draft" id="articleDraft")
        label.control-label(for="articleDraft") したがき
    .control-group
        .editor.control(v-editable="article.content" placeholder="ほんぶん")
    .control-group
        button.btn.btn-green.btn-block(type="submit") ほぞん
    .control-group(v-if="edit")
        button.btn.btn-red.btn-block(type="button" v-on="click: performDelete()") さくじょ
