h2.nomt カテゴリの管理
p
    a(href="/manage/category/new") カテゴリの追加

table.table(v-if="resolved")
    thead
        tr
            th 日本語
            th 英語
            th
    tbody
        tr(v-repeat="cat: cats" track-by="_id")
            td {{cat.name_ja}}
            td {{cat.name_en}}
            td
                a(href="/manage/category/{{cat._id}}") 編集
